/*1.Write a program that takes two integer inputs from the user and calculates their sum
using the addition operator (+). Display the result along with appropriate messages. */
#include<stdio.h>
void main()
{
  int a,b;
  printf("enter 2 values:");
  scanf("%d %d",&a,&b);

  printf("%d",a+b);

}
