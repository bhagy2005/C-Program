//5.write a c program that assign a value to a variable without declaring it first try to compile &observe the error.
#include<stdio.h>
void main()
{
 a=1;   
 printf("value:%d",a);
}
 // error:declare the variable so it shows error
