/*7.write a c program that: Declare & initialize the variables
                            Assign a new value to it
                            Print both  values(before and after assignment)*/
#include<stdio.h>
void main()
{

    int a=20;
        a=50;
    printf("a:%d",a);
}
// output is a:50 because assign new value to variable so it shows new value.
